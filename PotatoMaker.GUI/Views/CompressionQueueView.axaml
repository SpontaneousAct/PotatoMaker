<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PotatoMaker.GUI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="920" d:DesignHeight="720"
             x:Name="QueueViewRoot"
             x:Class="PotatoMaker.GUI.Views.CompressionQueueView"
             x:DataType="vm:CompressionQueueViewModel">

    <Design.DataContext>
        <vm:CompressionQueueViewModel/>
    </Design.DataContext>

    <Grid RowDefinitions="Auto,Auto,*"
          RowSpacing="12">
        <Border Grid.Row="0"
                Classes="pm-card pm-page-banner"
                Padding="16,14">
            <Grid ColumnDefinitions="*,Auto,Auto"
                  ColumnSpacing="12">
                <StackPanel Grid.Column="0"
                            Spacing="4">
                    <TextBlock Text="Compression Queue"
                               Classes="pm-page-title"/>
                    <TextBlock Text="{Binding QueueCountText}"
                               Classes="pm-section-title"/>
                    <TextBlock Text="{Binding QueueSummaryText}"
                               Classes="pm-caption"/>
                    <TextBlock Text="A running queue blocks one-off compression until the current batch finishes."
                               Classes="pm-caption"
                               IsVisible="{Binding IsBlockedByAnotherEncode}"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Compress all"
                        Command="{Binding CompressAllCommand}"
                        Classes="pm-primary"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"/>

                <Button Grid.Column="2"
                        Content="Clear queue"
                        Command="{Binding ClearQueueCommand}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1"
              ColumnDefinitions="3.1*,0.95*,2.1*,0.95*,0.85*,80"
              ColumnSpacing="12"
              IsVisible="{Binding HasItems}">
            <TextBlock Grid.Column="0"
                       Text="FILENAME"
                       Classes="pm-caption"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="1"
                       Text="SIZE"
                       Classes="pm-caption"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="2"
                       Text="PROGRESS"
                       Classes="pm-caption"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="3"
                       Text="OUTPUT"
                       Classes="pm-caption"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="4"
                       Text="TIME"
                       Classes="pm-caption"
                       FontWeight="SemiBold"/>
            <TextBlock Grid.Column="5"
                       Text="ACTIONS"
                       Classes="pm-caption"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="2"
              IsVisible="{Binding IsEmpty}">
            <StackPanel Margin="24"
                        Spacing="6"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="No videos queued yet."
                           Classes="pm-section-title"
                           TextAlignment="Center"/>
                <TextBlock Text="Prepare a clip on the main screen and use Add to queue to stage it here."
                           Classes="pm-caption"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="360"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="2"
                      IsVisible="{Binding HasItems}">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="vm:CompressionQueueItemViewModel">
                        <Border Classes="pm-card"
                                Margin="0,0,0,10"
                                Padding="16,14">
                            <Grid ColumnDefinitions="3.1*,0.95*,2.1*,0.95*,0.85*,80"
                                  ColumnSpacing="12">
                                <StackPanel Grid.Column="0"
                                            Spacing="4"
                                            MinWidth="0">
                                    <TextBlock Text="{Binding FileName}"
                                               Classes="pm-queue-file-name"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding InputPath}"
                                               Classes="pm-caption"
                                               TextTrimming="CharacterEllipsis"
                                               MaxLines="1"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1"
                                           Text="{Binding SelectedSizeText}"
                                           VerticalAlignment="Center"
                                           MinWidth="0"
                                           FontWeight="SemiBold"/>

                                <Grid Grid.Column="2"
                                      RowDefinitions="Auto,Auto"
                                      RowSpacing="6"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Stretch"
                                      MinWidth="0">
                                    <ProgressBar Minimum="0"
                                                 Maximum="100"
                                                 Value="{Binding ProgressPercent}"
                                                 HorizontalAlignment="Stretch"/>
                                    <TextBlock Grid.Row="1"
                                               Text="{Binding ProgressText}"
                                               Classes="pm-caption"
                                               HorizontalAlignment="Left"
                                               TextAlignment="Left"
                                               TextTrimming="CharacterEllipsis"
                                               MaxLines="1"/>
                                </Grid>

                                <TextBlock Grid.Column="3"
                                           Text="{Binding OutputSizeText}"
                                           VerticalAlignment="Center"
                                           MinWidth="0"
                                           TextTrimming="CharacterEllipsis"
                                           FontWeight="SemiBold"/>

                                <TextBlock Grid.Column="4"
                                           Text="{Binding ElapsedText}"
                                           VerticalAlignment="Center"
                                           MinWidth="0"
                                           FontWeight="SemiBold"/>

                                <Grid Grid.Column="5"
                                      ColumnDefinitions="36,36"
                                      ColumnSpacing="8"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Width="80">
                                    <Button Grid.Column="0"
                                            Classes="pm-queue-action"
                                            IsVisible="{Binding HasPrimaryAction}"
                                            ToolTip.Tip="{Binding PrimaryActionToolTip}"
                                            Command="{Binding PrimaryActionCommand}">
                                        <Grid Width="14"
                                              Height="14">
                                            <Path Classes="pm-control-icon"
                                                  Data="{StaticResource PmIconStopGeometry}"
                                                  IsVisible="{Binding CanCancel}"/>
                                            <Path Classes="pm-control-icon"
                                                  Data="{StaticResource PmIconArrowCounterclockwiseGeometry}"
                                                  IsVisible="{Binding CanRestart}"/>
                                        </Grid>
                                    </Button>
                                    <Button Grid.Column="1"
                                            Classes="pm-queue-action"
                                            ToolTip.Tip="Remove from queue"
                                            Command="{Binding RemoveCommand}">
                                        <Viewbox Width="14"
                                                 Height="14"
                                                 Stretch="Uniform"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center">
                                            <Path Classes="pm-control-icon"
                                                  Data="{StaticResource PmIconDismissGeometry}"/>
                                        </Viewbox>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
