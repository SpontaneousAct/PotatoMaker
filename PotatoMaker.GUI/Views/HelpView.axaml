<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PotatoMaker.GUI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="920" d:DesignHeight="720"
             x:Class="PotatoMaker.GUI.Views.HelpView"
             x:DataType="vm:HelpViewModel">

    <Design.DataContext>
        <vm:HelpViewModel/>
    </Design.DataContext>

    <UserControl.Styles>
        <Style Selector="TextBlock.pm-help-title">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="{DynamicResource PmHeadingForeground}"/>
        </Style>

        <Style Selector="TextBlock.pm-help-item-title">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PmSectionTitleForeground}"/>
        </Style>
    </UserControl.Styles>

    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="12"
                    Margin="0,0,0,12">
            <Border Classes="pm-card"
                    Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="Help"
                               Classes="pm-help-title"/>
                    <TextBlock Text="{Binding OverviewText}"
                               Classes="pm-muted"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Classes="pm-card"
                    Padding="16">
                <StackPanel Spacing="14">
                    <TextBlock Text="Start Here"
                               Classes="pm-section-title"/>

                    <ItemsControl ItemsSource="{Binding GettingStartedSteps}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="vm:HelpStep">
                                <Grid ColumnDefinitions="Auto,*"
                                      ColumnSpacing="12"
                                      Margin="0,0,0,8">
                                    <Border Classes="pm-help-badge"
                                            Width="28"
                                            Height="28"
                                            VerticalAlignment="Top">
                                        <TextBlock Text="{Binding Number}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontWeight="SemiBold"/>
                                    </Border>

                                    <StackPanel Grid.Column="1"
                                                Spacing="2">
                                        <TextBlock Text="{Binding Title}"
                                                   Classes="pm-help-item-title"/>
                                        <TextBlock Text="{Binding Description}"
                                                   Classes="pm-muted"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Classes="pm-card"
                    Padding="16">
                <StackPanel Spacing="14">
                    <TextBlock Text="Automatic Choices"
                               Classes="pm-section-title"/>

                    <ItemsControl ItemsSource="{Binding AutomaticChoices}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="vm:HelpNote">
                                <Border Classes="pm-inset"
                                        Margin="0,0,0,8">
                                    <StackPanel Spacing="2">
                                        <TextBlock Text="{Binding Title}"
                                                   Classes="pm-help-item-title"/>
                                        <TextBlock Text="{Binding Description}"
                                                   Classes="pm-muted"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Classes="pm-card"
                    Padding="16">
                <StackPanel Spacing="14">
                    <TextBlock Text="Quick Tips"
                               Classes="pm-section-title"/>

                    <ItemsControl ItemsSource="{Binding QuickTips}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="vm:HelpNote">
                                <Grid ColumnDefinitions="Auto,*"
                                      ColumnSpacing="12"
                                      Margin="0,0,0,8">
                                    <Border Classes="pm-help-badge"
                                            Width="24"
                                            Height="24"
                                            VerticalAlignment="Top">
                                        <TextBlock Text="i"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontWeight="SemiBold"/>
                                    </Border>

                                    <StackPanel Grid.Column="1"
                                                Spacing="2">
                                        <TextBlock Text="{Binding Title}"
                                                   Classes="pm-help-item-title"/>
                                        <TextBlock Text="{Binding Description}"
                                                   Classes="pm-muted"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Classes="pm-card"
                    Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Keyboard Shortcuts"
                               Classes="pm-section-title"/>

                    <ItemsControl ItemsSource="{Binding ShortcutHints}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="vm:ShortcutHint">
                                <Grid ColumnDefinitions="Auto,*"
                                      ColumnSpacing="12"
                                      Margin="0,0,0,8">
                                    <Border Classes="pm-inset"
                                            Padding="10,6"
                                            VerticalAlignment="Top">
                                        <TextBlock Text="{Binding Keys}"
                                                   FontWeight="SemiBold"/>
                                    </Border>

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Description}"
                                               Classes="pm-muted"
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
