<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PotatoMaker.GUI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="320" d:DesignHeight="260"
             x:Class="PotatoMaker.GUI.Views.VideoSummaryView"
             x:DataType="vm:VideoSummaryViewModel">

    <Design.DataContext>
        <vm:VideoSummaryViewModel/>
    </Design.DataContext>

    <UserControl.Styles>
        <Style Selector="TextBlock.pm-summary-title">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource PmSectionTitleForeground}"/>
        </Style>

        <Style Selector="TextBlock.pm-summary-caption">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Opacity" Value="0.82"/>
            <Setter Property="Foreground" Value="{DynamicResource PmMutedForeground}"/>
        </Style>

        <Style Selector="TextBlock.pm-summary-value">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
    </UserControl.Styles>

    <Border Classes="pm-card"
            Padding="10">
        <Grid RowDefinitions="Auto,Auto,Auto,Auto,*"
              RowSpacing="6">
            <TextBlock Grid.Row="0"
                       Text="Source Summary"
                       Classes="pm-summary-title"/>

            <Border Grid.Row="1"
                    Classes="pm-inset"
                    Padding="5,4">
                <Grid ColumnDefinitions="*,*,*,*"
                      ColumnSpacing="5">
                    <StackPanel Grid.Column="0" Spacing="0">
                        <TextBlock Text="Size" Classes="pm-summary-caption"/>
                        <TextBlock Text="{Binding FileSize, TargetNullValue=--}"
                                   Classes="pm-summary-value"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Spacing="0">
                        <TextBlock Text="Duration" Classes="pm-summary-caption"/>
                        <TextBlock Text="{Binding Duration, TargetNullValue=--}"
                                   Classes="pm-summary-value"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Spacing="0">
                        <TextBlock Text="Resolution" Classes="pm-summary-caption"/>
                        <TextBlock Text="{Binding Resolution, TargetNullValue=--}"
                                   Classes="pm-summary-value"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Spacing="0">
                        <TextBlock Text="FPS" Classes="pm-summary-caption"/>
                        <TextBlock Text="{Binding FrameRate, TargetNullValue=--}"
                                   Classes="pm-summary-value"/>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2"
                        Spacing="2">
                <TextBlock Text="Selected Clip"
                           Classes="pm-summary-title"/>

                <Grid ColumnDefinitions="*,*"
                      ColumnSpacing="4">
                    <StackPanel Grid.Column="0"
                                Spacing="0">
                        <TextBlock Text="Range" Classes="pm-summary-caption"/>
                        <TextBlock Text="{Binding SelectedRange, TargetNullValue=Full source file}"
                                   Classes="pm-summary-value"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Spacing="0">
                        <TextBlock Text="Length" Classes="pm-summary-caption"/>
                        <TextBlock Text="{Binding SelectedDuration, TargetNullValue=--}"
                                   Classes="pm-summary-value"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Row="3"
                        Spacing="2">
                <TextBlock Text="Strategy Preview"
                           Classes="pm-summary-title"/>
                <TextBlock Text="{Binding StrategyStatus, TargetNullValue=Select a source file to preview the plan.}"
                           Classes="pm-summary-caption"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <Grid Grid.Row="4"
                  ColumnDefinitions="*,*"
                  RowDefinitions="Auto,Auto,Auto"
                  RowSpacing="6"
                  ColumnSpacing="6">
                <StackPanel Grid.Row="0"
                            Grid.Column="0"
                            Spacing="0">
                    <TextBlock Text="Resolution" Classes="pm-summary-caption"/>
                    <TextBlock Text="{Binding StrategyResolution, TargetNullValue=--}"
                               Classes="pm-summary-value"/>
                </StackPanel>

                <StackPanel Grid.Row="0"
                            Grid.Column="1"
                            Spacing="0">
                    <TextBlock Text="Bitrate" Classes="pm-summary-caption"/>
                    <TextBlock Text="{Binding StrategyBitrate, TargetNullValue=--}"
                               Classes="pm-summary-value"/>
                </StackPanel>

                <StackPanel Grid.Row="1"
                            Grid.Column="0"
                            Spacing="0">
                    <TextBlock Text="Parts" Classes="pm-summary-caption"/>
                    <TextBlock Text="{Binding StrategyParts, TargetNullValue=--}"
                               Classes="pm-summary-value"/>
                </StackPanel>

                <StackPanel Grid.Row="1"
                            Grid.Column="1"
                            Spacing="0">
                    <TextBlock Text="Crop" Classes="pm-summary-caption"/>
                    <TextBlock Text="{Binding StrategyCrop, TargetNullValue=--}"
                               Classes="pm-summary-value"/>
                </StackPanel>

                <StackPanel Grid.Row="2"
                            Grid.ColumnSpan="2"
                            Spacing="0">
                    <TextBlock Text="FFmpeg filter" Classes="pm-summary-caption"/>
                    <TextBlock Text="{Binding StrategyFilter, TargetNullValue=--}"
                               Classes="pm-summary-value"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
