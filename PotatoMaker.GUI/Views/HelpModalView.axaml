<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PotatoMaker.GUI.ViewModels"
             x:Class="PotatoMaker.GUI.Views.HelpModalView"
             x:DataType="vm:HelpModalViewModel">

    <UserControl.Styles>
        <Style Selector="Grid#OverlayRoot">
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="Transitions">
                <Transitions>
                    <DoubleTransition Property="Opacity" Duration="0:0:0.20"/>
                </Transitions>
            </Setter>
        </Style>

        <Style Selector="Grid#OverlayRoot.open">
            <Setter Property="Opacity" Value="1"/>
            <Setter Property="IsHitTestVisible" Value="True"/>
        </Style>

        <Style Selector="Border#HelpCard">
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Transitions">
                <Transitions>
                    <DoubleTransition Property="Opacity" Duration="0:0:0.20"/>
                </Transitions>
            </Setter>
        </Style>

        <Style Selector="Border#HelpCard.open">
            <Setter Property="Opacity" Value="1"/>
        </Style>

        <Style Selector="Button.pm-help-close">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="{DynamicResource PmInsetBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PmInsetBorderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PmSectionTitleForeground}"/>
        </Style>
    </UserControl.Styles>

    <Grid x:Name="OverlayRoot"
          Classes.open="{Binding IsOpen}"
          Background="Transparent"
          PointerPressed="OnOverlayPointerPressed">
        <Border Background="#5F000000"/>

        <Border x:Name="HelpCard"
                Classes="pm-card"
                Classes.open="{Binding IsOpen}"
                Width="460"
                MaxWidth="460"
                Padding="16"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Grid RowDefinitions="Auto,Auto,Auto,Auto"
                  RowSpacing="10">
                <Grid Grid.Row="0"
                      ColumnDefinitions="*,Auto"
                      ColumnSpacing="8">
                    <TextBlock Text="Quick Guide"
                               Classes="pm-section-title"
                               FontSize="16"
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="X"
                            Classes="pm-help-close"
                            Command="{Binding CloseCommand}"
                            ToolTip.Tip="Close"/>
                </Grid>

                   <StackPanel Grid.Row="2"
                            Spacing="6">
                    <TextBlock Text="1. Drop in a video or click Browse."
                               Classes="pm-muted"
                               TextWrapping="Wrap"/>
                    <TextBlock Text="2. Preview the clip, then use Space to play or pause."
                               Classes="pm-muted"
                               TextWrapping="Wrap"/>
                    <TextBlock Text="3. Press A to mark the start and D to mark the end of the part you want."
                               Classes="pm-muted"
                               TextWrapping="Wrap"/>
                    <TextBlock Text="4. Check the summary, choose where to save, and pick GPU or CPU mode."
                               Classes="pm-muted"
                               TextWrapping="Wrap"/>
                    <TextBlock Text="5. Click Start Compression and watch the live progress log."
                               Classes="pm-muted"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <TextBlock Grid.Row="3"
                           Text="PotatoMaker tries to make a Discord-friendly file automatically. If your NVIDIA AV1 GPU encoder is available it uses that first; otherwise it falls back to CPU encoding."
                           Classes="pm-caption"
                           TextWrapping="Wrap"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
